/* configuration code

use toml format since it is pretty standard in rust

[colors]
  theme = dark

[keys]
  style = vim

[account]
  [account.name]
    imap_user = "user@host.com"
    smtp_url = ""
*/

